# relaunch-app

Kill and relaunch the running Snap My Windows app from the latest debug build.

## When to Use
When the user wants to restart the app without rebuilding — e.g., to test the current build again, or after a config change.

## Instructions

Run the relaunch script:
```bash
./scripts/relaunch.sh
```

Report whether the app was relaunched successfully.
